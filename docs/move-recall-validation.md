# 选子期间走法保留与语音精简验证

日期：2026-09-15。版本：1.1（2026091502）。

## 本次修复

语音只提交中文着法给系统合成器，例如“炮二平五”，不再添加“建议”。已经发布的合法走法单独保存原局面、原朝向和起终点。点选高亮、遮挡、延迟帧及候选局面确认期间，浮窗保留棋盘和“上一条走法”，用棕色虚线箭头区别于绿色实时建议。

确认实际新局面、改变朝向、手动同步和停止录屏后清除旧走法。同局计算可继续或复用完成结果；回看不会延长画面有效期，也不会把遮挡时刚算出的未发布结果冒充已确认建议。本次未更改识别阈值、棋盘适配范围、搜索预算或 NNUE。

## 自动化与视觉验收

- Xcode 26.6，iOS 26.5 项目专用模拟器，Release 配置。
- 完整 XCTest：107 项通过，0 失败；其中新增 7 项走法回看状态测试、5 项渲染测试。
- 覆盖选子遮挡、首张候选帧、确认一至两步后的清除、朝向和轮次变化、同步/停止重置、非法旧走法、当前建议优先、同局搜索缓存，以及现有识别与 TCP 传输回归。
- 已逐张检查红底“炮二平五”和黑底“马二进三”的原尺寸渲染附件：原棋盘、短着法、起点环、落点与虚线箭头均保留，显示“上一条走法 / 等待落子确认”。没有回看记录的过期棋盘仍不显示指令。
- Maestro 首页流程通过：启动、识别就绪、指定对局文案和设置可见，旧“我执”“练习设置”不存在；检查首页截图无异常。
- 因本次属于间歇性识别体验排查，按回归规则保留此前日志。测试、截图和日志均留在本地 `build/move-recall/`，不随源码提交。

## 打包与真机安装

Release 真机构建通过，主应用与录屏扩展版本均为 2026091502；应用整体签名验证通过，第三方 Zstandard BSD 许可已随应用打包。

2026-09-15 19:45（北京时间），`devicectl` 向已连接的 iPhone 17 Pro Max 返回安装成功。安装保留原应用数据。随后用户携带手机离开，本轮未继续启动录屏，也未在天天象棋中进行实际点选验收。

新安装包位于本地 `build/move-recall/XiangqiCoach-1.1-2026091502.ipa`。这是开发签名包，安装受设备与签名有效期限制。模拟器状态测试及渲染检查不替代真机连续录屏、系统语音实际听感和选子操作验收。
