# 重建版验收记录 · 2026-09-14

构建号：2026091402，版本：1.1。原有历史通过记录未计入本次结果。

## 自动验证

- Xcode 26.6，iOS 26.5 专用模拟器 `XiangqiCoach Rebuild 20260914`。
- Release 优化配置，`ONLY_ACTIVE_ARCH=YES ENABLE_TESTABILITY=YES CODE_SIGNING_ALLOWED=NO`：91 项 XCTest，0 失败，测试执行约 11 秒。
- XCTest 结果：`build/regression/release-tests-v2.xcresult`；日志：`build/regression/release-tests-v2.log`。
- 识别压缩矩阵：2 朝向 × 2 布局 × 3 高度 × 3 JPEG 质量，共 36 组，每组 90 个格子与真实标注一致。旧布局为几何重建，不是已丢失的旧原图。
- 实际两张用户原图经 macOS CoreImage/ImageIO 的 18 个清晰正例全通过，54 个模糊负例全部拒绝；详情见 `board-recognition.md`。
- 两张 Bundle 棋盘 → 真实 CoreImage 720 高/JPEG 0.5 → 识别 → 连续两帧确认 → 合法历史 → 真 NNUE 引擎 → 图形箭头全链路通过。黑底实图正确追溯 `h2e2` 并推导黑方行棋，白色起点光点保持空格。
- Release 模拟器识别平均 34 ms，10 次读数 32.4–38.1 ms；测试执行阶段无其他重型构建。该数字不是 iPhone 端到端延迟，帧传输、两帧确认和引擎预算需另计。
- 原生引擎：固定 12 层开局 `g3g4`、59,716 nodes 与原 Pro 对照记录一致；战术、取消恢复、错误输入和循环历史通过。直接 macOS 引擎/棋规 XCTest 另有 21 项通过。
- 回环 TCP 的分段、错误 JPEG、坏帧头、过期/倒序帧后继续接收通过。思考/遮挡保留棋盘，过期箭头撤销，超时终止与迟到结果隔离通过。
- 手动同步意图跨回教练/遮挡保留、两张新鲜帧确认、停止时失效等 8 项通过。

## 界面检查

- 目视检查红底/黑底实际引擎建议、等待识别、思考、上次确认局面，共 7 张 XCTest 附件。棋盘清晰，朝向和起终点正确，思考不清盘，过期不显示箭头或点击指令。
- 修复木纹取样含棋影造成的重复黑斑，取新实图河道中央干净原像素，14 个棋子资产未改变。
- 已检查模拟器首页。Maestro 实际点击“开启棋研录屏”后，系统 ReplayKit 日志确认 `buttonPressed` 与 `openControlCenterSystemRecordingView` 被触发；模拟器未出现可操作的直播选择面板，不能代替真机录屏验证。
- 录屏控件测试实际挂载 SwiftUI/ReplayKit 到 UIWindow，验证扩展标识、内部按钮有效尺寸和可点击区域。

## 构建调试记录

重建时修正了测试辅助闭包显式 self、SwiftUI 测试视图未挂载，以及 Release 测试缺少 enable-testing 标记。最终测试已全部通过。这些早期失败记录保留，未作为成功结果。

## 真机

当前检测到数据线连接的 iPhone 17 Pro Max，iOS 26.6.2，开发者模式已开启。23:19 已安装并成功启动构建 2026091401；手机诊断文件记录该构建号和新的会话 ID，录屏尚未开始。随后仅修正系统固定黑色录屏图标，最终构建 2026091402 的安装结果见文末。系统录屏启动需要用户本人确认，不能把静态图像或模拟器结果称为实机连续对局验收。

最终仅替换 ReplayKit 内部按钮的显示图像为红色系统符号，保留其原有事件；图像不再依赖某些版本不生效的 tint 继承。91 项核心回归后，另针对实际挂载的录屏控件运行增量验证。

录屏控件增量测试 1 项通过；最终使用预着色、alwaysOriginal 的系统符号，已通过真实模拟器截图确认红色显示，见 `build/regression/final-recording-button.png`。原生点击事件未更改。

最终安装完成：构建 2026091402 于 23:25 在数据线连接的 iPhone 上安装并成功启动；已读回该构建号、新的会话 ID 和 2026-09-14T15:25:08Z 的手机本地诊断。当前未开启录屏，未声称已完成实机连续对局测试。安装/启动/诊断证据保存在 `build/regression/phone-*-final.*`。
