# 录屏接收恢复检查（2026-09-15）

用户反馈的是此前一次无法识别。读取手机时，当前运行状态已改变；仅凭后来收到的最新快照，不能认定此前故障来自其他录屏应用。旧版只覆盖保存最新状态，已无法还原故障当刻的完整时序。

## 已确认的代码问题

- 接收端口短时不可用时，旧版监听失败后不会自行恢复。真实 Network.framework 占端口、释放端口实验确认：旧版释放后仍未恢复接收。新版串行管理监听生命周期，自动退避重试并屏蔽旧监听器回调。
- 首页仅凭系统录屏状态隐藏开始入口。系统录屏不代表已收到棋研扩展画面。新版以本会话近期有效帧区分等待、连接和断流；三秒无帧后恢复录屏入口，实际落子指引仍使用原有一秒有效期。
- 等待 PiP 就绪时断流会取消未完成启动。停止前的排队帧不能被后续会话当成首帧。

未调整棋子识别阈值、指定棋盘布局、红黑判定或引擎参数，也不把上述缺陷直接认定为用户此前那次故障的根因。

## 回归范围

沿用用户提供的指定木纹棋盘图与原始坐标测试。补充压缩像素序列：开局 → 合成选子或图形浮窗遮挡 → 恢复 → 下一步 h2e2；每帧均经过真实图像识别后进入棋局跟踪。合成场景和模拟器测试不等于手机故障现场或 ReplayKit 真机端到端验收。

本轮按间歇故障诊断保留日志；所有截图、原始诊断和构建结果留在忽略提交的 build/recognition-recovery/。

## 本地诊断

Documents/coach-diagnostics.json 仍是当前状态。新增 coach-diagnostics-history.json，最多保存 120 条阶段、连接、前后台、PiP 或已确认局面变化；持续变化的帧数和耗时不单独追加事件。记录仅留本机，不保存录屏图像、头像或屏幕原文。

## 已完成验证

- iOS 26.5 专用模拟器：完整 XCTest 181 项通过，0 失败；最终待启动取消条件定向复测 PiP 状态 17 项通过。
- Maestro 首页、二级帮助与原生录屏入口 2 条流程通过；检查首页截图，未连接状态恢复正常提示色。
- 模拟器实际运行已生成当前快照和有界历史文件；本轮读取历史为 7 条，结构完整。
- Release 真机构建及主应用/扩展签名校验通过。iPhone 17 Pro Max 已覆盖安装并启动，devicectl 独立查询确认版本 1.1（2026091508），启动后诊断也确认相同构建号。
- 本次安装同时包含上一版 1507 尚未安装的 PiP 左侧圆角安全边距修正。

刚才用户反馈的那次现场识别失败未复现。以上自动化与启动检查不替代一次完整的真机 ReplayKit 连续对局验收；本轮没有改动识别阈值或引擎参数。

