# 指定木纹棋盘验收 · 2026-09-15

构建号：2026091501。仅支持用户最新提供的红底开局、黑底 h2e2 后对局界面；“小白”练习布局已移除。引擎、权重与录屏传输沿用前一版。

## 输入依据

- 红图 1280×2781，标准红底，源图 SHA256 与之前红底样本一致。
- 黑图 1320×2868，黑底，红炮已走 h2e2，原位置白光为空。
- 独立 XCTest 图片仅保存棋盘裁剪，原点/尺寸/来源哈希/FEN 见 `XiangqiCoachTests/Fixtures/*.json`。裁剪逐像素与原图相等；完整原图仅在本机 build 证据目录中，测试资源不进入主应用或录屏扩展。

## 自动验证

两张完整原图分别经 macOS 正式识别器的 raw、720/JPEG0.5 路径，90 格与人工确认局面一致。原始/去均值特征对照 8/8 通过；额外 18 个清晰正例通过、54 个模糊负例全部拒绝。此处为主机验证，不能当成真机持续对局通过。

## 已确认的真机传输

2026091404 在后台开启实际 ReplayKit + PiP 时，本地诊断帧数从 125 → 421 → 1117 持续增长，单次传输读数约 7–59 ms；说明前一版固定端口零帧问题已恢复。读取真机 DVT 截图确认当时停在用户此次排除的“小白”练习界面，因此没有把它的识别失败算作指定棋盘验收。

本次只改善指定主题的局部阴影匹配，不加入其他布局，也不放宽颜色或清晰度门限。只读模板保持线程安全；每格提前去均值，模板比较内不增加重复统计。

## 最终构建与验收结果

- iOS 26.5 专用模拟器，Release XCTest：95 项全部通过、0 失败。两张独立实图恢复/压缩测试和双朝向浮窗阴影测试实际执行通过；原有模糊、遮挡、颜色、传输和引擎测试仍全部通过。结果：`build/specified-board/tests.xcresult`。
- Maestro 首页流程通过：识别就绪、对局指导文案、指导设置可见，“我执”和旧“练习设置”不可见；已目视检查首页截图，文字与录屏入口无截断遮挡。证据在 `build/specified-board/maestro`，原始日志因网络故障诊断而保留。
- 2026091501 Release 真机签名包生成成功。00:27 安装时 CoreDevice 返回设备不可用，随后设备列表再次确认 iPhone 为 `unavailable`；本版尚未安装，等待用户重新连接。当前手机仍为上一版，不能将模拟器结果说成新版本真机验收。
